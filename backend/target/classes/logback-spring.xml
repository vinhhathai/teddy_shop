<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 🎨 Modern Node.js-style Logging Configuration -->
    
    <!-- Pattern cho console giống Node -->
    <property name="LOG_PATTERN" 
              value="%cyan(%d{HH:mm:ss}) %highlight([%level]) %green(%logger{20}) - %msg%n"/>

    <!-- Console Appender with custom beautiful pattern -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} %highlight([%-5level]) %cyan(%logger{0}) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender với pattern đơn giản -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/shop-gau-bong.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%level] %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/shop-gau-bong.log.%d{yyyy-MM-dd}.gz</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Logging levels -->
    <logger name="com.gaubong.teddybearshop" level="debug" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>
    
    <logger name="org.springframework" level="info"/>
    <logger name="org.springframework.web.servlet.mvc.method.annotation.ExceptionHandlerExceptionResolver" level="error"/>
    <logger name="org.springframework.web.servlet.handler.AbstractHandlerExceptionResolver" level="error"/>
    <logger name="org.springframework.web.servlet.mvc.support.DefaultHandlerExceptionResolver" level="error"/>
    <logger name="org.hibernate" level="error"/>
    <logger name="com.zaxxer.hikari" level="warn"/>
    
    <root level="info">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>